import { Module } from '@nestjs/common';

/**
 * Placeholder Nest module exported by the generated auth library shell.
 *
 * @example
 * ```ts
 * imports: [NestAuthModule]
 * ```
 */
@Module({
  controllers: [],
  providers: [],
  exports: [],
})
export class NestAuthModule {}
