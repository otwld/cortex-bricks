import { Meta, Source } from '@storybook/addon-docs/blocks';
import sourceCode from 'source-loader:./connection-state.enum.ts';

<Meta title="websocket/ts/connection-state-enum" name="packagesWebsocketTSSrcLibEnumsConnectionStateEnumSource" />

# Connection State Enum

Source mirror for `packages/websocket/ts/src/lib/enums/connection-state.enum.ts`.

<Source code={sourceCode} language="ts" />
