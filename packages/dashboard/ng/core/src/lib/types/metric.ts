/**
 * File metric summary used by dashboard storage demos.
 */
export interface Metric {
  title: string;
  icon: string;
  fieldColor: string;
  color: string;
  files: string;
  fileSize: string;
  items: object;
}
