import type { Routes } from '@angular/router';

/**
 * Production websocket demo routes.
 *
 * The demo is intentionally disabled for production builds.
 */
export const websocketDemoRoutes: Routes = [];
