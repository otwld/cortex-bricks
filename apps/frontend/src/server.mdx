import { Meta, Source } from '@storybook/addon-docs/blocks';
import sourceCode from 'source-loader:./server.ts';

<Meta title="Applications/Frontend/Runtime/Server" name="appsFrontendSrcServerSource" />

# Server

Source mirror for `apps/frontend/src/server.ts`.

<Source code={sourceCode} language="ts" />
